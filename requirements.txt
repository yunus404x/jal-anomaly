# JAL-ANOMALY prototype - runtime dependencies
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
shap>=0.44          # if the installed shap and xgboost versions disagree, the
                    # pipeline falls back to xgboost's own TreeSHAP automatically
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# Optional. Installing torch switches the temporal detector from the EWMA
# baseline to the PyTorch LSTM-autoencoder named on the technical approach
# slide; everything else is unchanged.
# torch>=2.2

# Optional, only for scripts/screenshot.py (the console smoke test)
# playwright>=1.40
